/**
 * Sitewide positioning and how we use core SecOps terms in marketing copy.
 */

export const APP_URL = 'https://app.akinsec.com';

/** Lead line: product category + promise (SIEM-first, AI-assisted, approachable). */
export const MARKETING_THESIS =
  'AkinSec is a modern SIEM and security monitoring app—easier to run day to day—with AI-assisted workflows when you connect your own model providers.';

/** Secondary line: one-plane differentiation (use below the fold or paired with thesis). */
export const OPERATIONS_PLANE_TAGLINE =
  'One operations plane for SIEM, SOAR, and MDR—fewer consoles and clearer handoffs between detection and response.';

/** Short funnel line for CTAs and hero-adjacent copy. */
export const PRODUCT_SUBLINE = 'Sign in, connect sources, and monitor from app.akinsec.com.';

/** How AI shows up in the product (not chat-led). */
export const AI_WORKFLOW_DECK =
  'Summaries, triage help, reporting, and guided steps live inside monitoring workflows—not a standalone chat product.';

/** Supporting “bridge” story after SIEM is clear. */
export const POSITIONING_BRIDGE =
  'When you already run point tools, AkinSec brings signal and evidence together so audits and incident work do not scatter across tabs.';

/** FAQ-ready summary: BYO / customer-configured model APIs (no fabricated hosting claims). */
export const MODELS_FAQ_SUMMARY =
  'Model calls use AI providers you configure in the app (for example with your organization’s credentials or API keys where supported). Prompts and outputs are subject to your provider’s terms and settings; AkinSec orchestrates workflows, policies, and audit trails in the product.';

export const GLOSSARY = {
  SOAR: 'Orchestration for playbooks, case flow, and automated response actions.',
  SIEM: 'Log ingestion, correlation, and alerting across your sources.',
  MDR: 'Continuous monitoring and guided response for endpoints and cloud workloads.',
  AI_ASSISTED_MONITORING:
    'AI embedded in alerts, investigation, and reporting—not a chat-only surface.',
};
